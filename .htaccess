RewriteEngine On
RewriteBase /

# Redirige todas las peticiones que no sean a archivos o directorios existentes
# al archivo index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [L]